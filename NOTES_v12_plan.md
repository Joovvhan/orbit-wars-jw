# Orbit Wars — 진행 정리 & v12 접근법 (작성 2026-06-03)

내일 작업 시작점. 마감 2026-06-23 (약 21일), 하루 5제출, 최신 2개만 추적.

## 1. 현재 위치

| 에이전트 | vs v10 | vs ex_proto | vs ex_lb958 | vs ex_lb1200 | vs ex_lb1050a |
|----------|--------|-------------|-------------|--------------|---------------|
| v11 (현재 최신) | **58% (결정게임)** | 22.5% | 15% | 5% | **0%** |
| v9/v10 (이전) | — | ~0% | ~12% | — | 0% |

- **리더보드**: ex_lb1050a ≈ 1080, v7/v8 류 ≈ 580(μ₀ 근처 정체), **목표 ~1800**.
- v11 = v10 + "잉여 함대 hammer + multiprong"(travel≤12). v10은 확실히 이김, ex_proto 0→22.5% 향상. **하지만 최상위권(1050a/1200)엔 거의 전패.**
- **제출 준비 완료**: `main.py` = v11 (DEBUG=False, 스모크테스트 통과). 제출: `kaggle competitions submit orbit-wars -f main.py -m "..."`

## 2. 핵심 깨달음 (방향 전환의 근거)

### (A) 점수는 승/패/무만 반영 — 점수차 무관
- 약체를 크게 이기는 것보다 **다양한 상대에게 안정적으로 이기는 견고함**이 레이팅을 올린다.
- 1800점 = 필드 전반을 일관되게 이겨야 함. 한 상대 특화 튜닝은 비효율.

### (B) 연산 여유가 거대하다 — 이게 제일 중요
- 제한: 턴당 1초 + 게임당 60초 은행.
- v11 실측: **최악 턴 93ms, 평균 2~4ms, overage 0**. 즉 **턴당 ~900ms를 통째로 놀리는 중.**
- v8~v11은 전부 **단일 패스 그리디**. ex_lb1050a는 depth-2 search 사용.
- → **놀고 있는 연산을 탐색/롤아웃에 투입하는 것이 1800점으로 가는 가장 유망한 경로.**
- (주의: 벤치마크가 게임당 ~수십초 걸린 건 kaggle 엔진 비용이지 에이전트 비용 아님. 과거 오해 정정됨.)

### (C) 점진적 휴리스틱 튜닝의 한계
- v5→v11 동안 작은 ELO 향상만. 가중치/임계값 조정은 수익 체감.
- 반복된 함정: "주력 행성을 적립용으로 벤치에 앉히면 회귀" (v8, v10, v11 초기 모두). 기억해 둘 것.

## 3. v12 후보 접근법 (내일 결정)

1. **탐색 기반 정책 (최우선 후보)**
   - depth-2~3 lookahead: 내 수 → 상대 최선 반응 → 평가. melis를 이걸로 확장/대체.
   - 또는 **경량 롤아웃**: 양쪽을 단순 정책으로 게임 끝까지 시뮬 → 승패로 평가. 후보 수마다 N회 롤아웃.
   - 예산: 턴당 500ms 안전. 수백 평가/롤아웃 가능.
   - 리스크: 액션 공간이 연속(각도·함대수)이라 분기 설계 필요 → "유망 후보 수 생성 → 탐색"으로 이산화.

2. **ex_lb1050a 정독 후 이식** (3798줄)
   - depth-2 search, mega-hammer 누적, multiprong, personality(patient/opportunistic/pressure) 전환의 *정확한* 구현 파악.
   - v11의 hammer가 약한 이유(잉여로만, 규모 제한) 대비 어떻게 대규모 집중을 안전하게 적립하는지.

3. **자기대전 파라미터 최적화 (CMA-ES 등)**
   - 휴리스틱 가중치를 자동 튜닝. 단독으론 한계(접근법 B/C와 병행 시 가치).

4. **4p FFA 대응 점검**
   - 현재 전부 1v1 가정. 평가에 4p FFA 포함되므로 다인전 로직(누구를 칠지, 어부지리 회피) 별도 필요할 수 있음.

## 4. 인프라 (준비됨)
- `benchmark.py` / `sweep_v11.py` / `eval_vs.py` 모두 기본 워커 = 논리코어/2 (이 머신 16). `-j`로 조절.
- `eval_vs.py hero opp1 opp2 ...` : hero vs 상대리스트만 평가(ex-vs-ex 낭비 없음). 양방향.
- 벤치 회수 표준(제안): 스크리닝 n=12(24게임), 확정 n=30(60게임), examples n=15~20.
- 결과 JSON: `results/`.

## 5. 내일 첫 액션 (제안)
1. ex_lb1050a 전략 정독 → 구조 메모.
2. 탐색/롤아웃 프로토타입 1개를 v11 위에 얹어 PoC (턴 예산 내 동작·승률 확인).
3. v11은 이미 제출 가능 상태 — 필요시 오늘/내일 제출해서 실제 레이팅 피드백 시작.
